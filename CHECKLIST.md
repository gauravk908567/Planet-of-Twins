# CHECKLIST (temporary — user working board, 2026-07-17)

> Temporary doc, explicitly user-authorized (overrides the no-doc-proliferation rule for this one file). DELETE this file once all items are checked off.
>
> Reorganised 2026-07-27: three buckets — **✅ Done** (struck through) · **⬜ Remaining** (do now, in between) · **⏳ After playtest** (deferred). Item numbers preserved from the original board.

## ✅ Done (struck through)

- [✅] ~~1. Per-zone terrains in main levels — sculpt + 4 layers max + assign `M_PoTTerrain` + paint details (author foliage at max density, dense-where-it-reads) - ✅~~
- [✅] ~~2. Roads/paths on terrain: flatten strip → gravel bed layer → decals for edges/markings → slab meshes + curbs (fantasy-city vocabulary, not modern) ✅~~
- [✅] ~~3. Place flora/rock prefabs (bushes/ferns/rocks/trees) per zone; stone-slab walkways ryokan-style ✅~~
- [✅] ~~4. Dress zones with the 9 decorative props (+ lanterns on `WindSwayPivot` hooks once wired) ✅~~
- [✅] ~~5. SkyboxMountains horizon ring per zone, tinted toward fog colour ✅~~
- [✅] ~~6. Fog.vfx wisps at crack mouths, shrine bases, under decks ✅~~
- [✅] ~~7. Cloud heightmap into skybox `_CloudTex` DONE 2026-07-18: clouds.png @ 0.6 — swap the texture on M_CoexistenceSkybox if you prefer another ✅~~
- [✅] ~~8. `LocalWindZone`s at cracks (cloth/ropes whip near energy leaks) + WindDriver dials to taste (Direction/Strength/Gusts) ✅~~
- [✅] ~~9. Water: try `WaterStream`/`WaterLake` where zones want it (L0 canals especially) ✅~~
- [✅] ~~10. APV + reflection-probe bake per zone — Baking Set incl. Persistent; bake AFTER buildings/rocks placed (flora irrelevant to bake). **STEP-BY-STEP: SETUPGUIDE §16** ✅~~
- [✅] ~~11. Verify crack meshes don't have Contribute GI static flag before baking ✅~~
- [✅] ~~60. Occlusion culling bake per area scene (occluder/occludee marking rules + verify steps): **SETUPGUIDE §17** ✅~~
- [✅] ~~12. Volumetric fog: `M_PoTVolumetricFog` Start Distance 8→20–30, Density to taste; Sun In-Scatter if shafts double up ✅~~
- [✅] ~~13. Built-in fog: WorldAmbienceDriver `_fogDensity` 0.006→~0.002 or untick Drive Fog ✅ (Optional: Not implemented currently)~~
- [✅] ~~14. Crack: judge streaks-off look; Current Strength→0 if stream still bad; HDR colour-picker Intensity +1–2 for more bottom glow ✅~~
- [✅] ~~17. Terrain shader dials on `M_PoTTerrain`: Hex Grid Scale / Blend Sharpness / Parallax Strength / Height Transition ✅~~
- [✅] ~~18. Wind material opt-ins: `_WindAmount/_WindResponse` per swaying material; `WindSwayPivot` Max Angle/Frequency on lanternsp[Requires Rework]~~
- [✅] ~~20. Hex validation vs ArtStyle §10 clan ramps in Unity (old standing item)~~
- [✅] ~~22. Manpu: vocab `loopPrefab`s (Enraged/Panicked/Aggressive) + `CorruptionStateCueBook` assigned on EnemyDarkEnergy prefabs + `radorb_cast` burst + Manpu 8-glyph art (long-standing)~~
- [✅] ~~23. `SpawnPrewarmProfile` asset rows (P16 — pool warm-up counts)~~
- [✅] ~~25. Melee pickups in L1_Park are authored inactive — confirm what should activate them, or activate~~
- [✅] ~~45. Terrain detail material: `M_PoTDetailGrass` CREATED (PoT/DetailFoliage + Grass_A texture) — assign on detail prototypes ("Vertex Lit" render mode + GPU instancing ON), duplicate per detail texture (fern/bush/heather from Art/Terrain/Details_Demo), _WindAmount ~0.08 grass / 0 pebbles; paint, then scrub corruption to confirm grass stains with terrain~~
- [✅] ~~46. Fabric ripple check: ClothBanner_A / Ribbon_A / Ofuda_A meshes now subdivided (486v) — confirm their material is PoT/Coexistence with non-zero `_WindAmount` (+ Hanging wind mode if the material has it), watch them ripple in play~~
- [✅] ~~27. P12 TestLab round-2 (Ctrl+`, window drag, FX stop buttons)~~
- [✅] ~~28. P13 tutorial DoD: Bootstrap progressive-unlock run + all four entry paths~~
- [✅] ~~35. L1_Park terrain: TRIM LAYERS — currently 13–16 layers (4 alphamaps) on `New Terrain 1`. UPDATE 2026-07-17: PoT add-pass shader BUILT — layers 5–8 now get full PoT features (hex/parallax/corruption). New rules (SETUPGUIDE §3): layers 1–4 broad coverage, 5–8 ACCENTS only (2nd geometry pass), HARD CAP 8, distant terrains ≤4. So trim L1 from 13–16 down to ≤8 (ideally 4+accents)~~
- [✅] ~~37. Terrain layers referencing SAMPLE-PACK textures RESOLVED 2026-07-17: layers + textures moved GUID-intact to Assets/Art/Terrain/ — L1 references survive, no rebuild needed~~
- [✅] ~~38. Festival time-of-day rig (`FestivalLookTest` in SampleScene + Settings/Grading/Tests/) — delete once golden-hour/dusk states are authored in the real scenes [Deleted but need the golden hour etc to work]~~
- [✅] ~~48. L2_Streets skybox: still the AllSkyFree "Epic_GloriousPink" HDRI (source of the off-bible pink) — DECIDE: swap to M_CoexistenceSkybox like the other levels (recommended; driver/moon/corruption then work there too) or keep the HDRI~~
- [✅] ~~49. CAMERA FOV: gameplay vcams 40 → ~52 (biggest single scale-feel fix; judge world scale ONLY after this, through the gameplay camera — never in Scene view next to a capsule)~~
- [✅] ~~51. L1 before/after terrains: duplicate Before TerrainData → edit wounds into the After copy; two Terrain GOs under the existing timeline state parents; NavMeshSurface per state (bake each while active); blocked paths = VISIBLE wreckage + collider in the After group (never invisible wall alone) [Requires intense reqork maybe later completed according to current scope]~~
- [✅] ~~52. Streets roads: flatten strip → slab meshes + curbs (kit pieces) → URP Decal Projectors for cracks/markings/wear; dug-up areas = sharp Set Height sink + rubble layer paint + lip decal ring (true see-through holes = Paint Holes brush, shader supports it) [Requires intense reqork maybe later completed according to current scope]~~
- [✅] ~~53. Temple-mountain vista: one bespoke luminous peak at the SAME azimuth in every scene (Erdtree principle) + SkyboxMountains ring fog-tinted (item 5) — Fable greyboxes the vista prefab; you place per scene + lantern-chain the path, frame vistas through gates~~
- [✅] ~~55. VFX DRAW-ON-TOP — BUILT 2026-07-18 (SETUPGUIDE §12): tick "Draw On Top" on ground-telegraph cue elements (cast circles, meteor decals); verify one in play (stand in tall grass, fire the cue, ring reads through). Use sparingly~~
- [✅] ~~56. TEMPLE VISTA + METRICS GYM staged in SampleScene (SETUPGUIDE §14–15): judge the vista from ground level in play, dial the 5 M_Vista* materials, then place per real scene at the same azimuth; walk twins past MetricsGym for the scale pass (after FOV #49); KitPreview_Row (inactive) = browse the 51 kit pieces [checked and the temple vista doesn't exist anymore as it has been removed and i already created my own]~~
- [✅] ~~54. LOCAL FOG VOLUMES: `PoT/LocalFogVolume` shader BUILT — **reworked 2026-07-24 to a true raymarched 3D wind-driven box volume** (was a flat sheet); make a material, put it on a scaled default Cube (collider off) over paths/hollows/crack mouths (recipe + dials + verification in SETUPGUIDE §5B/§5D); it drifts with WindDriver (in Play) and stains with corruption; **re-tune `_Density` (~0.3 → ~0.5+) on placed instances after the rework**; keep 2–3 overlapping max per view. (SETUPGUIDE §5 also now documents the CristianQiu global god-ray fog + the whole fog-systems map.)~~
- [✅] ~~44. GIT UPDATE after improvement items land (commit the sprint: grades, terrain shader, moves, moon, props)~~
- [✅] ~~57. DRAW-ON-TOP AUTHORING GAP (found 2026-07-19): the mechanism is built (item 55) and the `GroundVFX` layer exists, but **ZERO cue elements have the flag ticked** — no `.asset` in the project serializes `drawOnTop: 1`. Go through the telegraph/marker cues (chain marker `On_TetherChainMarker` + ghost `on_chainMarker`, VoidStrike hazard zones, Witness ritual circle, cast circles, meteor decals) and tick "Draw On Top" on each ground-telegraph element in the Cue Book editor, then verify one in play (item 55's test)~~
- [✅] ~~62. CRACK GRADIENT still unbuilt: Pure Current→Khal-Vor depth gradient on `Shader Graphs/CrackGlow` (`_ColourA/_ColourB` + `_Corruption` float + UV.y ramp + slow pan) — the current violet-magenta is the documented clash (SETUPGUIDE §18 bottom; canon = Colour Bible §7)~~
- [✅] ~~64. TERRAIN LAYER Mask Maps for the PX packs (PavingStoneBrick etc.): pack AO(G)+displacement(B)+glossiness(A) into ONE PNG (sRGB OFF), assign per layer — enables per-layer AO/roughness/height on the existing shader, zero shader changes (game.md §17.3; import rule sources in chat 2026-07-20). Optional: Fable builds an auto-pack editor tool~~
- [✅] ~~65. BUILD-FOR-TESTERS GATE (do these IN ORDER before any tester build): (a) commit the world sprint (item 44) · (b) discard M_SunShafts.mat (item 61) · (c) Build Settings order = Bootstrap(0), Persistent, Intro, then areas — TestLab/Restore/Trees/SampleScene NOT in list · (d) debug keys: L/O/P/I/K + DamageDealerDebug are DevConfig.Trainer/editor-gated — verify Trainer OFF in the build · (e) all four entry paths smoke-run (CLAUDE.md Unity Workflow) · (f) player-facing runbook for testers = TESTGUIDE.md; tool usage reference = TESTGUIDE §C tool shelf~~
- [✅] ~~40. Motion blur is now IN the grade profiles (0.2 warm → 0.5 shock) — judge in play; delete the override from profiles if you hate it~~
- [✅] ~~41. Corruption-colour unification DONE 2026-07-17: all three fog/sky corruption colours → (0.26, 0.07, 0.19) Voreth-family-lifted; eyeball once in play~~
- [✅] ~~31. Delete `TwinHouse_Fantasy` (rejected cute)? Old `AccordTwinHouse_A` at (32,0,0)?~~
- [✅] ~~32. `Assets/Shader/VolumetricLights/` third-party folder — keep or remove~~
- [✅] ~~33. Sample-pack deletion — extraction DONE 2026-07-17 (keepers in Assets/Art/Terrain/ + Assets/Art/Shaders/UGUI_SG_Samples). Safe to delete: `TerrainDemoScene_URP`, `TerrainSampleAssets`, and `Assets/Samples/Shader Graph` (Common/Custom Lighting/Production Ready/Terrain Shaders). Check console after each delete; expect execute_code to start working again after~~
- [✅] ~~42. MOON (new in CoexistenceSkybox shader, default OFF): preview with `_MoonIntensity` 1.6 · `_MoonSize` 0.14 · `_MoonDir` (-1, 0.2, 0.05) · `_MoonHalo` 1.4 on the dusk sky — tune size/detail/veil by eye; it dims with corruption and clouds pass in front automatically ✅~~
- [✅] ~~58. BLUE-HOUR / AFTER-STATE GRADING NOT WIRED (verified 2026-07-19, twice): nothing connects the After-terrain timeline state to `Grade_EarlyFear` ("blue dusk wake-up"). Persistent's `WorldAmbienceDriver._driveGrading` = 0, the `SkyboxMaterialChange._gradeId` = empty, L1_Park contains neither component. To wire: either a `SkyboxMaterialChange` with `_gradeId = early_fear` on the After state parent, or turn on `_driveGrading` with progress in EarlyFear's window (≥0.15) — plus optionally `SkyStateDriver.BlendTo(dusk)` for the sky itself (item 39's seam) — UPDATE 2026-07-24: `SkyboxMaterialChange` (item 67, now with a sky channel) IS this wiring mechanism; put one on the After-state parent set to `_skyStateId = dusk` + raise corruption + `_gradeId = early_fear` ✅~~
- [✅] ~~61. POST-PROCESS "which volume does what" + tonemapping rules (never a 2nd Tonemapping override outside grade profiles): **SETUPGUIDE §18** (full table ArtStyle §11, numbers §11.2). Sun shafts: nothing to set up; BUG-077 standing rule = `git checkout -- Assets/Art/Materials/M_SunShafts.mat` before EVERY commit ✅~~

## ⬜ Remaining (do now — in between)

- [ ] 19. Area identity volumes: profiles AUTHORED + wiring VERIFIED in all 4 scenes (2026-07-17) — only tune to taste in play 
- [ ] 21. Grayscale world test pass
- [ ] 26. Human play-run: ESC chain, pause mutes audio, control hints keys, camera occlusion fade, hold-B freeze + 4s cooldown, pickup prompts, rescue ring faces camera while player rotates (UIBillboard lazy-fix), no "-nan frustum" console spam after unpause (BUG-074 fix)  [Requires Substantial fixes]
- [ ] 29. Grade profile tuning — LOCKED VALUES APPLIED 2026-07-17 (all 6 grades + ACES + MotionBlur): play-scrub SetStoryProgress 0→1 and tune by eye; Act1 = golden hour, EarlyFear = blue dusk wake-up [Not workingi n game]
- [ ] 30. Post-Opus graphics options menu play-check (incl. any rows left for manual Inspector hookup) [Nothing is there need fix]
- [ ] 36. L1 TerrainData lives at `Assets/New Terrain 1.asset` — move/rename into `Assets/Scenes/L1_Park/` alongside navmesh/occlusion (folder-per-scene convention)
- [ ] 39. Dusk skybox state — BUILT 2026-07-17: `SkyStateDriver` in Persistent + SkyState_Day/GoldenHour/Dusk assets (Settings/Sky/). Eyeball: select the SkyStateDriver GO in play → ContextMenu "TEST: Apply golden_hour" / "TEST: Blend to dusk (5s)" (moon comes on with dusk); tune the three state assets to taste. Story wiring calls ApplyState/BlendTo later (same seam as StoryGradeDirector)
- [ ] 47. RyokanInn lanterns: prefab now has LanternHook_L/R (WindSwayPivot 8°/1.1 Hz) + hanging HollowLantern_A ×2; SampleScene copy replaced with a real prefab instance (left INACTIVE like the other props) — activate, verify sway (needs WindDriver in scene), tune hook angle/frequency/drop (−0.45 m) to taste; optionally add 2 more hooks at rear corners [Requires Reword]
- [ ] 68. VERIFY VIDEOS PRESENT & PLAYING: (a) the **ability preview videos show in the Skill Tree UI** — each ability node's preview MP4 is assigned and actually renders; (b) the **tutorial videos** are present and play in the tutorial. NB these are the `VideoPlayer→WMF→D3D12` clips flagged in memory `project_videoplayer_d3d12_crash` (skillvideos/*.mp4) — check in a **real build, not just the editor**, and if the build switches Graphics API to DX11 (per shipping advice), re-confirm they still play and don't crash.

## ⏳ After playtest (deferred)

- [ ] 15. Corruption pacing: Persistent SkyboxChanger → Drive Corruption off/low (~0.02); place beat objects in later areas instead [To be done later after playtest]
- [ ] 16. New corruption *film* look: scrub WorldAmbienceDriver progress and judge the blood-moon tint on houses+terrain; per-material `_CorruptionStrength` = film opacity [To be done later after playtest]
- [ ] 24. Upgrade-tier cue art: `<id>_tN` variants in books when tier art exists (P15 system live, ids all serve base for now)[TO BE DONE LATER]
- [ ] 34. `rescue.md` / `WIRING_GUIDE.md` root docs — fold into game.md or keep [Not done should be done later]
- [ ] 43. RAIN SYSTEM — BUILT 2026-07-18 (SETUPGUIDE §6): `RainSystem` in Persistent (drops w/ world collision + ripple/splash sub-emitters + wetness global + audio child). YOU: play-test via RainController ContextMenu (Drizzle/Heavy/Stop), tune drop/ripple material tints, assign a rain-loop audio clip, place RainDripLine emitters under eaves — **INVESTIGATED 2026-07-25:** wiring is CORRECT (collision World + all-layers enabled, ripple/splash wired as collision sub-emitters `type:1`, all 3 mats assigned, drops reach ground: life 2.4s/grav 1.6 ≈ 45m fall vs 12m). Ripples/splashes barely fire because collision **`quality: Medium`** uses an approximate voxel cache that fast rain tunnels through. **DECIDED 2026-07-25 (keep Medium):** on `RainDrops` → Collision, keep Quality = Medium, set **Voxel Size 0.5 → 0.2** (finer grid catches the fast drops, no per-particle-raycast cost increase); if still sparse try 0.15, then the two-layer split (cheap non-colliding visual rain + sparse ~150-rate High-collision layer owning the sub-emitters). Manual — it's a setting inside the Persistent WIP scene. [Will be checked after playtest]
- [ ] 50. Prop scale pass vs the metrics table (game.md/chat 2026-07-17): doors 2.4–2.6 · storeys 3.4–4.0 · shrine 4.5–6 · torii 5–7 · lanterns 2.6–3.2 · bushes 0.6–1.2 (SMALL — ground texture, not walls) · trees 8–15 (tall = shafts) · banners 2.5–3.5 drop; Fable does the measured L1 audit after your terrain pass [we will see later after play test this item] [Will be seen and action will be taken only after the playtest response]
- [ ] 59. UI REVAMP round 2: the authoritative spec now lives in **game.md §17.5** (art inventory, one-channel-one-meaning ruling, broken-heart emblem numbers, trough remaps, HUD layout, world-UI games-reference table, ability icon/panel spec incl. clan-tint toggle + centre-out cooldown + ready-flash, round-1 gotchas). Built mechanisms are on branch `ui-swap-2026-07-19` — recover with `git checkout ui-swap-2026-07-19 -- <path>`, don't rebuild. ANY agent doing UI work reads §17.5 FIRST [Next round after the playtest]
- [ ] 63. TEXTURE-ARRAY terrain sampling (MicroSplat's real feature): NEEDED if terrains stay >8 layers (L1 was 13–16, item 35). Either trim to ≤8 per item 35 OR schedule the `Texture2DArray` rewrite of PoTTerrainLit sampling (all layers same res/format + array-builder tool). Decision pending — don't start without picking one [will be done after playtest]
- [ ] 66. UI INTEGRATION (after round-2 look sign-off; spec = game.md §17.5 FINALISED block): (a) QTE ring — CODE SEAM DONE 2026-07-21 (QTEManager auto-uses `UIRingTimerView` when the anchor's TimerRing has one, legacy path otherwise); REMAINING = authoring: on each area's QTE anchor TimerRing Image assign `M_UIRingTimer_QTE` + add `UIRingTimerView` (do per scene while you're in it — scenes are your WIP, Fable won't save them) · (b) ability icons: grey-when-cooling + centre-out colour creep over the SYMBOL + ready flash (`PlayReadyFlash`) · (c) BILLBOARD AUDIT — every world-space canvas (enemy bars, twin/soul bars, rescue ring, mash/QTE anchors, pickup prompts) carries `UIBillboard` · (d) dormant→available flash on shared emblem + Weaver's Gate · (e) health bars recovered from `ui-swap-2026-07-19` (Fable's stack, approved) [After playtest more will be done]
- [ ] 67. STORY-BEAT WIRING — `SkyboxMaterialChange` (the repurposed story-beat trigger, `Assets/Scripts/Environment/`, SETUPGUIDE §8) fires sky (SkyStateDriver — sky channel added 2026-07-24) + corruption (WorldAmbienceDriver) + grade (StoryGradeDirector). Fires on Timeline Activation (Act 9 SkyboxChanger) / Start / code `Fire()`. YOU: place one per beat (golden→crack→dusk per §8; this IS the wiring item #58 flagged); set `_skyStateId = dusk` on the beat that should go dusk. **Sun-direction control also new:** assign the Persistent directional light to `SkyStateDriver._sunLight` + author `sunDir` per `SkyState_*` asset (rotates the real light → disc + shadows move together; default (0,0,0) = untouched). Play/test all three channels + grayscale via GameDebuggerV2 `Ctrl+\`` → World&Sky / Story grading / Grayscale sections. NOT persisted across Restart yet (needs a CheckPointManager StoryProgress field). Open: add GameDebuggerV2 to Persistent (dev-gated) so those benches work while walking real areas? Post-playtest: rename `SkyboxMaterialChange`→`StoryBeatTrigger` (GUID-preserving, game.md §20.3). [More will be done after playtest correct for now]
